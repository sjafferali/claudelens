# API Configuration
# For production (Docker) or when frontend/backend are served together:
VITE_API_URL=/api/v1

# For local development with separate backend server:
# VITE_API_URL=http://localhost:8000/api/v1

# API Key (optional for local development)
VITE_API_KEY=
