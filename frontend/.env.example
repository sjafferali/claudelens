# API Configuration
# For production or external hosts, use relative URL (recommended):
# VITE_API_URL=/api/v1
# For local development with separate backend:
# VITE_API_URL=http://localhost:8000/api/v1

# API Key (optional for local development)
VITE_API_KEY=